<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.Dao.HelloDao" >

    <select id="getSomeThing" resultType="java.lang.String" parameterType="java.lang.String">
        SELECT
        district_name
        FROM
        zhengwugongkai.acc_district
        WHERE
        id = #{id}
    </select>
</mapper>
